spring:
  application:
    name: todo
  datasource:
    url: jdbc:h2:mem:tarefas
    username: sa
    password: aula
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
