spring:
  application:
    name: estacionamento
  datasource:
    url: jdbc:h2:mem:produtos
    username: sa
    password: aula
  jpa:
    database-plataform: rg.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
